<%= form_with(model: supported_locale, local: true) do |form| %>
  <% if supported_locale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supported_locale.errors.count, "error") %> prohibited this supported_locale from being saved:</h2>

      <ul>
        <% supported_locale.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
